# Server Configuration
SERVER_PORT=8080

# Basic Authentication Credentials
# IMPORTANT: Replace with strong, unique credentials
AUTH_USER=admin
AUTH_PASS=changeme

# PostgreSQL Database Connection URL
# Example: postgres://user:password@host:port/dbname?sslmode=disable
DATABASE_URL=postgres://user:password@localhost:5432/syncdocs?sslmode=disable

# GitHub Personal Access Token (PAT)
# Required for interacting with the GitHub API.
# Generate one here: https://github.com/settings/tokens
# Needs 'repo' scope to access private/public repository contents.
GITHUB_TOKEN=your_github_pat_here

# Sync Interval for background checks
# Go duration format (e.g., 1h, 30m, 2h45m)
# Defaults to 1h if not set or invalid
SYNC_INTERVAL=1h
